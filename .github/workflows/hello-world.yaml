name: hello world
on:
  push:
    path:
      - 'docs/**.go'
    branches: [main]
jobs:
  say-hello: 
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello World 
      - run: echo "TODAY is $(date)" 
      - run: echo Heello world. $date